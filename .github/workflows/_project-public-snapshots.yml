name: Retired public market snapshot projection

on:
  workflow_call:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm the public data pipeline is retired
        run: echo "Public market snapshot projection is retired; no data artifact is created."
