name: Retired legacy data-cache publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm the legacy publisher is inert
        run: echo "The public data-cache publisher is retired; owner data is never pushed to Git."
